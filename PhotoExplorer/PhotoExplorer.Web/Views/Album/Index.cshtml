@model IEnumerable<PhotoExplorer.Web.Models.AlbumViewModel>

@{
    ViewBag.Title = "Index";
}

<div class="container">
    <h2>Explore Albums</h2>
    @{
        foreach (var album in Model)
        {
            @Html.Partial("_Album", album);
        }
    }
</div>
