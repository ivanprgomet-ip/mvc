@model  IEnumerable<PhotoExplorer.Web.Entities.AlbumEntityModel>

@{
    foreach (var album in Model)
    {
        foreach (var photo in album.Photos)
        {
            @Html.Partial("_Photo", photo)
        }
    }
}